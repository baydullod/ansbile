---
- name: Check the hostname of all hosts
  hosts: all
  tasks:
    - name: Get the hostname
      ansible.builtin.command:
        cmd: hostname
      register: result

    - name: Display the hostname
      ansible.builtin.debug:
        msg: "Hostname: {{ result.stdout }}"
